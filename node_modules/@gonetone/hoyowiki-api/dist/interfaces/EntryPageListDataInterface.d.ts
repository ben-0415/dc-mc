export interface EntryPageDataOptions {
    pageNum?: number;
    pageSize?: number;
    filters?: string[];
}
export interface EntryPageListData {
    get(options: EntryPageDataOptions): Promise<any>;
    getList(...filters: string[]): Promise<any>;
    getTotal(...filters: string[]): Promise<number>;
    searchListByName(name: string): Promise<any>;
}
